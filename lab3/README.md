## Телеграм-бот задач (python-telegram-bot)

Дружелюбный бот для постановки задач, приоритета и дедлайна. Напоминает за 14 дней, 7 дней и в день дедлайна. Хранение задач — в `tasks.json`. Также радует пользователей случайными шутками! 🎭

### Возможности
- Поставить задачу (текст)
- Установить приоритет (`low`, `medium`, `high`)
- Установить дату дедлайна (`ГГГГ-ММ-ДД`)
- Посмотреть все задачи
- Удалить задачу
- Напоминания: за 14 дней, 7 дней и в день дедлайна в 09:00 локального времени
- **Новое!** При создании задачи бот отправляет случайную шутку (setup), а через 10 секунд приходит punchline
- **Новое!** Команда `/getrandomjoke` для получения случайной шутки в любое время

### Требования
- Python 3.10+
- Токен Telegram-бота (`BOT_TOKEN`)

### Установка и запуск
1. Клонируйте проект или поместите файлы в директорию.
2. Создайте виртуальное окружение (рекомендуется):
```bash
python -m venv .venv
.venv\Scripts\activate  # Windows
```
3. Установите зависимости:
```bash
pip install -r requirements.txt
```
4. Создайте файл `.env` по примеру `.env.example` и укажите `BOT_TOKEN`.
   Опционально укажите `TIMEZONE` (по умолчанию `Europe/Moscow`).
5. Запустите бота:
```bash
python bot.py
```

### Команды
- `/start` — краткая справка
- `/newtask` — новый диалог создания задачи (текст → приоритет → дедлайн)
- `/setpriority` — изменить приоритет существующей задачи
- `/setdeadline` — установить/изменить дедлайн задачи
- `/list` — посмотреть свои задачи
- `/deletetask` — удалить задачу
- `/getrandomjoke` — получить случайную шутку (setup + punchline в одном сообщении)
- `/cancel` — отменить текущий диалог

### Хранение данных
- Файл `tasks.json` создаётся автоматически в корне проекта.
- Формат: список объектов `{task_id, chat_id, text, priority, deadline}`.

### Напоминания и часовой пояс
- Напоминания планируются при создании/изменении задач и при старте бота.
- Время отправки — 09:00 локального часового пояса `TIMEZONE`.
- На Windows требуется пакет `tzdata` (уже включён в зависимости).

### Обработка ошибок
- Логи пишутся в стандартный вывод.
- Если `TIMEZONE` некорректный — будет использован `Europe/Moscow`.
- Если `tasks.json` повреждён — бот продолжит работу с пустым списком задач.
- При ошибках запроса к API шуток бот продолжит работу, просто не отправит шутку в этом случае.

### API для шуток
Бот использует API [official-joke-api.appspot.com](https://official-joke-api.appspot.com/random_joke) для получения случайных шуток. Шутки автоматически добавляются в уведомления о создании задач и доступны по команде `/getrandomjoke`.

### Частые вопросы
**Q:** Бот не отправляет напоминания?  
**A:** Проверьте, что дедлайн в будущем, бот запущен, указан корректный `TIMEZONE`, и системное время верное.

**Q:** Как удалить дедлайн?  
**A:** В команде `/setdeadline` отправьте `-` вместо даты.

**Q:** Бот не присылает шутку при создании задачи?  
**A:** Возможно, произошла временная ошибка при запросе к API. Попробуйте создать задачу ещё раз или используйте команду `/getrandomjoke` для получения шутки вручную.


